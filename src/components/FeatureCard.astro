---
interface Props {
  iconSvg: string;
  title: string;
  description: string;
  delay?: string;
}

const { iconSvg, title, description, delay = '0s' } = Astro.props;
---

<div class="feature-card animate-on-scroll" style={`--delay: ${delay}`}>
  <div class="feature-icon">
    <Fragment set:html={iconSvg} />
  </div>
  <h3>{title}</h3>
  <p>{description}</p>
</div>
